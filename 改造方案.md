# Humanizer 中文化改造方案(v1.0,已定稿)

决策已全部确认,正在执行。本文件是临时草稿,不属于 skill 包,完成后可删除。

## 一、目标与范围

把当前面向英文的 Humanizer 改造成一份**中文优先、纯净、可移植**的 skill:

- 提示词、模式、例子、词表全部按中文 AI 文本的真实特征重写。
- **不上架任何商店/平台**:删除全部 marketplace 相关内容。
- **不绑定任何 agent**:删除全部 OpenAI、Claude Code 相关内容。
- 保留原 skill 的底层理论、四步工作流、voice 匹配、三种输出模式、"何时不动手"、MIT 许可。
- 额外:在 QwenWork 里配一个 `/说人话` slash command 作为唤起入口(独立于 skill 仓库,见第七节)。

## 二、本轮三条优化的处理

### 点 1+2:去掉 marketplace 与 agent 绑定

整目录/整文件删除:

- `.claude-plugin/marketplace.json`(marketplace)
- `.claude-plugin/plugin.json`(Claude Code 插件)→ 连带删 `.claude-plugin/` 目录
- `agents/openai.yaml`(OpenAI)→ 连带删 `agents/` 目录

连带清理(这些文件里有引用):

- `README.md`:删 skills.sh 徽章、`npx skills add` 安装、`/plugin marketplace add` + `/plugin install`(Claude Code)、Claude Desktop ZIP 上传。只保留一句**中立的手动安装**说明("把 SKILL.md 或整个文件夹拷进你 agent 的 skill 目录")。
- `scripts/validate-package.py`:删 plugin.json 的读取与 `PLUGIN.get("version")`/`PLUGIN.get("skills")` 校验;版本号一致性改为只在 `SKILL.md` 与 `README.md` 两处比对。
- `AGENTS.md`:key-files 清单删掉 plugin.json/marketplace.json/openai.yaml 三条;"Version" 规则改成两处同步;"Checks" 删掉 `npx skills add . --list` 与 `claude plugin validate .`,只留 `python3 scripts/validate-package.py`。
- `.github/workflows/validate.yml`:删 `npx skills add` 与 `claude plugin validate` 两步及 setup-node;是否保留精简版 CI 见决策点 6。

### 点 3:用 `/说人话` 唤起

- QwenWork 里 **skill ≠ slash command**。skill 靠 description 自动触发或 Skill 工具调用,不会自动变成 `/名字`;slash command 是 `~/.qwenworkcn/commands/` 下独立的 `.md` 文件,文件名=命令名。
- 做法:**新建命令文件** `~/.qwenworkcn/commands/shuorenhua.md`,正文指示 agent 对粘贴文本套用 humanizer-cn skill。该文件在 QwenWork 配置目录、**不在 skill 仓库内**,所以 skill 仍然纯净、不绑定 agent。
- skill 的 `name` 字段用 ASCII `humanizer-cn`,因为 QwenWork skill 命名要求小写字母/数字/连字符。
- 命令名用 ASCII `/shuorenhua`(已确认不用中文名),符合 QwenWork 命令文件"小写字母和连字符"的约定,解析无悬念。

## 三、决策(已确认)

1. **skill `name`**:`humanizer-cn`(与仓库同名,和已装英文版共存)。
2. **改造策略**:就地重写成中文版,版本升 **4.0.0**。
3. **模式排序**:保留 A–E 五组结构与 1–25 编号(组顺序本就按"强度+动作类型"排列;全量重编号会牵动所有 `§` 引用、README 表格与校验,风险大收益小)。"中文频率"的洞察用在内容侧重、词表与例子上,不打乱编号。§8/§10/§12/§20/§21 换内容但编号不变。
4. **模式数量**:维持 25 条,能折进现有模式就不新增。
5. **README 章节标题**:用中文 `## 25 个模式`,并同步改校验脚本里那一行硬编码字符串。
6. **CI 工作流**:精简成只跑 `python3 scripts/validate-package.py`(删 npx/claude 两步与 setup-node)。
7. **唤起命令**:用 ASCII 名 `/shuorenhua`,命令文件放 `~/.qwenworkcn/commands/`(仓库外),不用中文名。

## 四、逐条模式改造清单

图例:✅ 直接翻译+换中文例子 / ⚠️ 概念保留但表面形式要重写 / 🔁 整条替换成中文专属特征

### A. 摆架子而不直说(1–5)

| # | 模式 | 处理 | 中文里的样子 |
|---|---|---|---|
| 1 | 不是X而是Y | ✅ | "不是……而是……""不仅仅是……更是……""这不只是一次升级,这是一场变革";截尾式"……,无需猜测" |
| 2 | 一行式收尾/碎句 | ✅ | "这,才是真正的关键。""值得深思。""请记住这一点。";碎句"没有退路。没有借口。";口号单独成段 |
| 3 | 假深沉格言 | ✅ | "本质上""归根结底""真正的核心在于""……的底层逻辑""对称是信任的语言" |
| 4 | 铺垫开场 | ✅ | "让我们一起来看看""话不多说""众所周知""值得一提的是""不得不说""接下来我们将" |
| 5 | 跟空气辩论 | ✅ | "这并不是说……""需要澄清的是""有人可能会认为……但其实……""一种看似可行的做法是……" |

### B. 按规则造节奏(6–11)

| # | 模式 | 处理 | 中文里的样子 |
|---|---|---|---|
| 6 | 强行三段式/排比 | ✅(中文更严重) | "创新、灵感与洞察";"既……又……还……";段落级排比三连;**折入**顿号长串堆砌、"首先/其次/再次/最后"机械分段 |
| 7 | 重复句首 | ✅ | 中文主语可省,更多表现为重复关联词/状语:"同时……同时……""通过……通过……" |
| 8 | 破折号 | ⚠️改规则 | 中文破折号(——)是合法标点,**不能照搬"禁用"**。改为"限频,煽情停顿优先用逗号/句号";补省略号(……)拖尾、感叹号滥用。代码/路径中的符号不动 |
| 9 | 堆叠限定词 | ✅ | "可能在某种程度上或许会""据推测大概也许""在一定程度上不排除" |
| 10 | 连字符词对 | 🔁替换 | 中文无连字符。替换为"**四字格/成语堆砌 与 ……化名词串**":"数字化、智能化、生态化""全方位、多层次、宽领域""方兴未艾、如火如荼、应运而生"连用 |
| 11 | 被动语态 | ⚠️改焦点 | 中文被动少。真正对应的是"**名词化弱动词/无主句**":"对……进行了优化""实现了……的提升""得到了广泛的应用" |

### C. 夸大与借权威(12–18)

| # | 模式 | 处理 | 中文里的样子 |
|---|---|---|---|
| 12 | AI 高频词表 | 🔁整表换 | **最大改动**。换成:赋能、抓手、闭环、颗粒度、心智、对齐、拉通、沉淀、纵深、格局、生态、链路、维度、破局、护城河、增长飞轮、底层逻辑、顶层设计、组合拳、发力、深耕、卡位、势能、久久为功、行稳致远、蔚然成风、应运而生、脱颖而出、举足轻重、不可或缺、至关重要、日益、愈发、彰显、凸显、勾勒、谱写、铸就、擘画、提质增效。注明:这是唯一词表,表外书面词本身不算 AI 味 |
| 13 | 拔高意义 | ✅(中文更严重) | "标志着……的重要里程碑""具有深远意义""为……奠定坚实基础""开启……新篇章""书写浓墨重彩的一笔""尽管面临挑战……依然蓬勃发展""未来可期""前景光明""迈上新台阶" |
| 14 | 含糊关联 | ✅ | "与……有着千丝万缕的联系""与……息息相关""深度绑定""有机结合" |
| 15 | -ing 尾巴 | ⚠️改形式 | 中文对应"**结尾拔高小句**":"……,充分体现了……""……,彰显了……""……,折射出……""……,印证了……" |
| 16 | 营销腔 | ✅ | "坐落于""宛如璀璨明珠""令人叹为观止""美不胜收""流连忘返""独具匠心""匠心打造""极致体验""一站式""殿堂级""网红打卡地""不容错过" |
| 17 | 借权威 | ✅ | "专家指出""业内人士表示""权威数据显示""相关研究表明""备受瞩目""广受好评""屡获殊荣" |
| 18 | 回避简单动词 | ⚠️改焦点 | 中文对应滥用"进行/开展/加以/予以/对……作出/实现":"对方案进行了优化"→"优化了方案";"予以重视"→"重视" |

### D. 按规则排版(19–21)

| # | 模式 | 处理 | 中文里的样子 |
|---|---|---|---|
| 19 | 装饰性加粗 | ✅ | Markdown 加粗滥用、每个列表项加粗标签+冒号(与语言无关) |
| 20 | 装饰性标题 | ⚠️改内容 | 去掉英文"title case"(中文无大小写);保留 emoji/箭头(→)/分隔线滥用;补中文特有:【】方括号标题、"(一)(二)/一二三"层级堆叠 |
| 21 | 弯引号 | 🔁替换 | **方向反了**:中文标准就是全角弯引号 "",禁用会帮倒忙。替换为"**全角半角/中英标点混用**":中文里混半角标点( , . : " )、中英标点混排、引号内多余空格 |

### E. 聊天/草稿残留(22–25)

| # | 模式 | 处理 | 中文里的样子 |
|---|---|---|---|
| 22 | 机器人客套 | ✅(中文更严重) | "希望对您有所帮助!""当然可以!""这是一个非常好的问题!""如需进一步展开请告诉我""需要我继续吗?" |
| 23 | 知识边界免责声明/猜测 | ✅ | "截至我的知识更新时间""根据现有资料""由于相关信息有限""未见公开报道""据推测,他可能出生于……" |
| 24 | 标题被首句复述 | ✅ | 与语言无关 |
| 25 | 描述旧版本 | ✅ | 与语言无关(文档/代码注释) |

### 中文专属、但建议"折进"而非新增的特征

- 机械分段"首先/其次/再次/最后/综上所述" → 折入 §6 或 §2
- 中英混排(中文句里硬插英文词:"这个 feature 的 performance 很 good") → 折入 §12 或 §21
- "综上所述/总而言之"式收尾 → 折入 §2 或 §13

## 五、文件级改动清单

| 文件 | 改动 |
|---|---|
| `SKILL.md` | 全文中文化:理论、工作流、voice、输出模式、25 条模式+例子、"何时不动手"、来源。YAML 键名保持英文;`name` 见决策点 1;`description` 写成中文(或中英),保证中文请求能自动触发。**≤400 行**(当前 375) |
| `README.md` | 中文化;**删全部商店/agent 安装说明**,只留中立手动安装;重写一个**中文完整前后对比示例**(英文版是里斯本游记,要换中文语境);版本历史加 4.0.0、保留旧条目;章节标题见决策点 5;来源部分诚实标注"在英文来源基础上做中文本地化与扩充" |
| `scripts/validate-package.py` | 删 plugin.json 相关校验;版本一致性改为 SKILL.md ↔ README 两处;改 `## The N patterns` 那行硬编码(决策点 5);其余正则(`### N.` 标题、`| N |` 表格、≤400 行)与语言无关,保留 |
| `AGENTS.md` | key-files 删 plugin/marketplace/openai 三条;Version 规则改两处同步;Checks 只留 python 校验;补中文 Plain Language 原则与"模式内容/词表/标点按中文重建"说明 |
| `.github/workflows/validate.yml` | 删 npx/claude 两步与 setup-node;精简或整体删除见决策点 6 |
| `LICENSE` | 不动 |
| **删除** `.claude-plugin/`(plugin.json + marketplace.json) | 整目录移除 |
| **删除** `agents/`(openai.yaml) | 整目录移除 |
| **新建**(仓库外)`~/.qwenworkcn/commands/shuorenhua.md` | QwenWork slash command,正文指示套用 humanizer-cn skill |

## 六、校验与版本

- **版本号**:重大行为变更,升到 **4.0.0**。删除 plugin.json 后,只需两处一致——`SKILL.md` 的 `metadata.version` 与 README 第一条版本历史。
- **模式编号**:从 1 连续、无缺口。已确认保留 A–E 五组与 1–25 编号,§8/§10/§12/§20/§21 只换内容不换号,所以无需全量重编号;`§` 引用、README 表格编号天然保持一致。校验脚本从 `### N.` 标题反推数量。
- **发布前检查**(精简后):只跑 `python3 scripts/validate-package.py`。本机是 Windows,需确认 `python3` 可用(记忆里提过 Python 脚本要加 `PYTHONUTF8=1` 防 GBK 崩溃);跑不了我会如实说明。

## 七、关于 `/shuorenhua` 命令(仓库外)

命令文件 `~/.qwenworkcn/commands/shuorenhua.md` 草案:

```markdown
---
description: 把 AI 味的中文改写得像人写的
---

把用户在这条命令后面提供的文本,套用 humanizer-cn skill 改写:
标记 AI 特征 → 起草改写 → 检查草稿 → 给出终稿。
保留事实,不编造;若用户给了写作样例,按样例语气改写。
```

- 放 `~/.qwenworkcn/commands/`,**不进 skill 仓库**,保证 skill 纯净可移植。
- 用法:输入框敲 `/shuorenhua` 再粘贴文本。
- 依赖:命令引用 `humanizer-cn` 技能,需先把本仓库装进 QwenWork 的 skill 目录(见 README 手动安装)。

## 八、风险与取舍

- **命令依赖技能**:`/shuorenhua` 引用 `humanizer-cn` 技能,需先把本仓库装进 QwenWork 的 skill 目录,命令才生效。
- **行数上限**:SKILL.md ≤400 行,词表和新特征会挤占,边写边数。
- **来源权威性**:中文 AI 特征缺乏与英文维基同等的权威清单,主要靠重写+中文语境观察;README 来源要诚实标注本地化与扩充,不假装中文有同源清单。
- **忠实度**:§8/§10/§12/§20/§21 必须按中文重建,直译会不伦不类。
- **voice 匹配**:中文语感(长短句、标点节奏、成语密度)与英文不同,voice 一节要补中文说明。
- **删 CI/插件后**:失去自动校验与一键安装,但你不上架、纯本地用,影响可忽略。

## 九、实施步骤(定稿后执行)

1. 你确认决策点 1–7。
2. 删除 `.claude-plugin/`、`agents/` 两目录。
3. 做 §12 中文词表和其他高频清单(价值最高、最费斟酌)。
4. 重写 SKILL.md 全部模式与例子,盯住 ≤400 行;`name`/`description` 按决策点定。
5. 为 README 写中文完整前后对比示例;中文化 README;删商店/agent 安装说明。
6. 改 validate-package.py(删 plugin 校验、改标题字符串)、AGENTS.md、validate.yml。
7. 两处版本号统一升到 4.0.0,写 README 版本历史条目。
8. 跑 `python3 scripts/validate-package.py`(加 `PYTHONUTF8=1`),修编号/表格/行数问题。
9. 新建 `~/.qwenworkcn/commands/shuorenhua.md`(引用 humanizer-cn 技能)。
10. 通读:Plain Language、无编造事实、`§` 引用一致。
